# Netlify configuration for Next.js App Router using the official Netlify Next.js plugin
# The plugin will manage the proper publish directory and function packaging.
[build]
  command = "npm run build"
  publish = ".netlify/output"

[[plugins]]
  package = "@netlify/plugin-nextjs"
